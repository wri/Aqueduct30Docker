hello world

build docker:
`docker build -t friendlyhello testDocker`

cleanup

check containers
`docker pd -a`


`docker stop <containerID>`
`docker rm <containerID>`

check images
`docker images`
`docker rmi <imageID>`
